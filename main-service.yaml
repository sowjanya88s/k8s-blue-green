apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: LoadBalancer
  selector:
    # labels are used as selectors
    project: roboshop
    tier: frontend
    component: frontend
    version: blue
  ports:
  - protocol: TCP
    port: 80 # servie port
    targetPort: 80 # container port
